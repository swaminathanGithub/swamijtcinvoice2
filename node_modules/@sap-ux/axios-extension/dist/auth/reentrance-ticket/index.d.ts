import type { Logger } from '@sap-ux/logger';
import { ABAPVirtualHostProvider } from './abap-virtual-host-provider';
/**
 * Get the reentrance ticket from the backend.
 *
 * @param options options
 * @param options.backendUrl backend Url
 * @param options.logger  logger
 * @param options.timeout timeout in milliseconds
 */
export declare function getReentranceTicket({ backendUrl, logger, timeout }: {
    backendUrl: string;
    logger: Logger;
    timeout?: number;
}): Promise<{
    reentranceTicket: string;
    backend?: ABAPVirtualHostProvider;
}>;
//# sourceMappingURL=index.d.ts.map